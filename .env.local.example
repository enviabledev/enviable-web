# Copy to .env.local and adjust as needed. Never commit .env.local.
#
# Backend API base URL. The Next app proxies same-origin /api/* requests here.
# Default targets the local backend dev server (from enviable-system) on port 3000.
# Override for a different host or for the Fly.io deployment later.
BACKEND_API_URL=http://localhost:3000
